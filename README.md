# Backend API (minimal ASP.NET Core Web API)

This is a minimal C# ASP.NET Core Web API scaffold.

Quick run:

```bash
cd backend-api
dotnet restore
dotnet run --urls "http://localhost:5000"
```

Test the health endpoint:

```bash
curl http://localhost:5000/api/health
```
